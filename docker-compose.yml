services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: ${REGISTRY:-ghcr.io}/${NAMESPACE:-jnardiello}/printfarmhq:backend-${VERSION:-v1.5.0}
    container_name: printfarmhq-backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    # No environment variables needed - using database-based configuration
    networks:
      - printfarmhq-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        DOCKER_ENV: "true"
    image: ${REGISTRY:-ghcr.io}/${NAMESPACE:-jnardiello}/printfarmhq:frontend-${VERSION:-v1.5.0}
    container_name: printfarmhq-frontend
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - DOCKER_ENV=true
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - printfarmhq-network

networks:
  printfarmhq-network:
    driver: bridge
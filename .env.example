# Docker Registry Configuration
# These variables control where Docker images are pulled from/pushed to

# Registry URL (default: ghcr.io for GitHub Container Registry)
REGISTRY=ghcr.io

# Namespace for images (default: printfarmhq)
# Set this to your GitHub username or organization
NAMESPACE=printfarmhq

# Version tag for images (default: latest)
VERSION=latest

# GitHub Token for pushing to registry
# Create one at: https://github.com/settings/tokens
# Required scopes: write:packages, read:packages
# GITHUB_TOKEN=your_token_here